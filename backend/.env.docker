# Database — menggunakan nama container 'db' sebagai hostname
# PENTING: Di dalam Docker network, gunakan nama container (db) bukan localhost!
DATABASE_URL=postgresql://postgres:12112004@db:5432/emandor

# JWT
SECRET_KEY=ganti-dengan-random-string-panjang-minimal-32-karakter
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60

# CORS
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173